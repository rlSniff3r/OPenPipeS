---
tipo: index
projeto: {{ project_name }}
gerado_em: {{ generated_at }}
---

# 🗂️ Index — {{ project_name }}

## Navegação Rápida

- [[Sync/Dashboard_Global.md|📊 Dashboard Global]]
{%- for t in targets %}
- [[Sync/{{ t.name }}.md|🖥️ {{ t.name }}]] — {{ t.ips[0] if t.ips else "—" }} ({{ t.vuln_count }} vulns, {{ t.open_ports_count }} portas)
{%- else %}
- *Nenhum alvo registrado*
{%- endfor %}

---

## Resumo de Alvos

| # | Alvo | IP | Portas | Endpoints | Vulns |
|---|------|----|--------|-----------|-------|
{%- for t in targets %}
| {{ loop.index }} | [[Sync/{{ t.name }}.md\|{{ t.name }}]] | {{ t.ips[0] if t.ips else "—" }} | {{ t.open_ports_count }} | {{ t.endpoint_count }} | {{ t.vuln_count }} |
{%- else %}
| — | *Nenhum alvo* | — | — | — | — |
{%- endfor %}

---

*Gerado pelo OPenPipeS Sync em {{ generated_at }}*
